export const environment = {
  production: true,
  apiUrl: 'api/'//1. add this, why it's different form environment.ts?
  // because when we launch our site, it will be hosted and served from our .net core app

};
